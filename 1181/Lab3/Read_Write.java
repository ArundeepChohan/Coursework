import.java*util;
public static void main(string[] args) throws exception
{
	File inputs =new File("input.txt");
	if(inputs.exists())
		System.exit(0);
	File targFile= new File("output.txt");
	if(!output.exists())
		System.exit(0);
	Printwriter output =new Printwriter(targFile);
	Scanner inputs =new Scanner(inputs);
	while(inputs.hasNext())
	{
		command=inputs.next();
		//
		//
	}
	output.close();
	input.close();
}